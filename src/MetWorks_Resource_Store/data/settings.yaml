values:
  - path: /services/udpListener/preferredPort
    value: "50222"

definitions:
  - path: /services/jsonToPostgreSQL/connectionString
    defaultValue: "Host=192.168.8.9;Port=5432;Database=weather;Username=weather;Password=password"
    description: "Connection string for PostgreSQL database"
    expectedValueType: "String"
    allowableValues:
    isSecret: true

  - path: /services/jsonToPostgreSQL/enableBuffering
    defaultValue: "false"
    description: "Enable buffering for PostgreSQL database"
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isSecret: false

  - path: /services/instance/installationId
    defaultValue: ""
    description: "Per-installation stable identifier (GUID). Generated on first run and persisted locally."
    expectedValueType: "String"
    allowableValues: []
    isEditable: false
    isSecret: false

  - path: /services/jsonToSQLite/connectionString
    defaultValue: ""
    description: "SQLite connection string (leave blank to use dbPath + default connection string builder)."
    expectedValueType: "String"
    allowableValues:
    isEditable: true
    isSecret: true

  - path: /services/jsonToSQLite/dbPath
    defaultValue: "metworks.sqlite"
    description: "SQLite database file path (relative paths are relative to the app's data directory)."
    expectedValueType: "String"
    allowableValues:
    isEditable: true
    isSecret: false

  - path: /services/jsonToSQLite/enableBuffering
    defaultValue: "false"
    description: "Enable buffering for SQLite writes when the database is unavailable (e.g., locked or I/O errors)."
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isEditable: true
    isSecret: false

  - path: /services/loggerFile/fileSizeLimitBytes
    defaultValue: "10485760"
    description: "File size limit in bytes"
    expectedValueType: "Int32"
    allowableValues: []
    isSecret: false

  - path: /services/loggerFile/minimumLevel
    defaultValue: "Information"
    description: "Minimum log level"
    expectedValueType: "String"
    allowableValues:
      - "Verbose"
      - "Debug"
      - "Information"
      - "Warning"
      - "Error"
      - "Fatal"
    isSecret: false

  - path: /services/loggerFile/outputTemplate
    defaultValue: "[InstallationId: {InstallationId}{Timestamp:yyyy-MM-dd HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}{Exception}"
    description: "Serilog output template"
    expectedValueType: "String"
    allowableValues: []
    isSecret: false

  - path: /services/loggerFile/relativeLogPath
    defaultValue: "Logs/log-.txt"
    description: "File path for log files"
    expectedValueType: "String"
    allowableValues: []
    isSecret: false

  - path: /services/loggerFile/retainedFileCountLimit
    defaultValue: "7"
    description: "Retained file count limit"
    expectedValueType: "Int32"
    allowableValues: []
    isSecret: false

  - path: /services/loggerFile/rollOnFileSizeLimit
    defaultValue: "false"
    description: "Roll on file size limit"
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isSecret: false

  - path: /services/loggerFile/rollingInterval
    defaultValue: "Day"
    description: "Rolling Interval"
    expectedValueType: "String"
    allowableValues:
      - "Infinite"
      - "Year"
      - "Month"
      - "Day"
      - "Hour"
      - "Minute"
    isSecret: false

  - path: /services/loggerPostgreSQL/autoCreateTable
    defaultValue: true
    description: "Auto create table for PostgreSQL logging"
    expectedValueType: "Boolean"
    allowableValues: []
    isSecret: false

  - path: "/services/loggerPostgreSQL/connectionString"
    defaultValue: "Host=192.168.8.9;Port=5432;Database=weather;Username=weather;Password=Sylvia3927"
    description: "Logging Connection string for PostgreSQL database"
    expectedValueType: "String"
    allowableValues: []
    isSecret: true

  - path: /services/loggerPostgreSQL/minimumLevel
    defaultValue: "Information"
    description: "Minimum log level for PostgreSQL logging"
    expectedValueType: "String"
    allowableValues:
      - "Verbose"
      - "Debug"
      - "Information"
      - "Warning"
      - "Error"
      - "Fatal"
    isSecret: false

  - path: /services/loggerPostgreSQL/tableName
    defaultValue: "log"
    description: "Table name for PostgreSQL logging"
    expectedValueType: "String"
    allowableValues: []
    isSecret: false

  - path: /services/loggerSQLite/autoCreateTable
    defaultValue: true
    description: "Auto create table for SQLite logging"
    expectedValueType: "Boolean"
    allowableValues: []
    isSecret: false

  - path: /services/loggerSQLite/dbPath
    defaultValue: "metworks-log.sqlite"
    description: "SQLite database file path for logging (relative paths are relative to the app's data directory)"
    expectedValueType: "String"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/loggerSQLite/minimumLevel
    defaultValue: "Information"
    description: "Minimum log level for SQLite logging"
    expectedValueType: "String"
    allowableValues:
      - "Verbose"
      - "Debug"
      - "Information"
      - "Warning"
      - "Error"
      - "Fatal"
    isSecret: false

  - path: /services/loggerSQLite/tableName
    defaultValue: "log"
    description: "Table name for SQLite logging"
    expectedValueType: "String"
    allowableValues: []
    isSecret: false

  - path: /services/metrics/applicationId
    defaultValue: "00000000-0000-0000-0000-000000000000"
    description: "Stable application identifier (GUID) used for metrics correlation across app renames"
    expectedValueType: "String"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/metrics/autoCreateTable
    defaultValue: "true"
    description: "Auto create table for PostgreSQL metrics summaries"
    expectedValueType: "Boolean"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/metrics/captureIntervalSeconds
    defaultValue: "10"
    description: "Metrics collection interval in seconds"
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/metrics/connectionString
    defaultValue: "Host=192.168.8.9;Port=5432;Database=weather;Username=weather;Password=password"
    description: "Metrics persistence connection string for PostgreSQL database"
    expectedValueType: "String"
    allowableValues: []
    isEditable: true
    isSecret: true

  - path: /services/metrics/enabled
    defaultValue: "false"
    description: "Enable metrics collection and persistence (intended for wall-device tuning; typically off for field deployments)"
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isEditable: true
    isSecret: false

  - path: /services/metrics/pipelineEnabled
    defaultValue: "false"
    description: "Enable provenance-based pipeline timing summaries for weather readings (UDP→transform and transform durations)"
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isEditable: true
    isSecret: false

  - path: /services/metrics/pipelineTopN
    defaultValue: "10"
    description: "Number of top reading types to include in periodic pipeline summary"
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/metrics/relayEnabled
    defaultValue: "false"
    description: "Enable EventRelayBasic handler timing aggregation"
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isEditable: true
    isSecret: false

  - path: /services/metrics/relayTopN
    defaultValue: "10"
    description: "Number of top (message_type, recipient_type) handler pairs to include in periodic relay metrics summary"
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/metrics/storageEnabled
    defaultValue: "true"
    description: "Capture local storage consumption metrics (settings overrides, log file, logging sqlite, readings sqlite)"
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isEditable: true
    isSecret: false

  - path: /services/metrics/storageTopN
    defaultValue: "10"
    description: "Maximum number of largest log files (by size) to include under the log file sink directory"
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/metrics/tableName
    defaultValue: "metrics_summary"
    description: "Table name for PostgreSQL metrics summaries"
    expectedValueType: "String"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/streamShipping/enabled
    defaultValue: "false"
    description: "Enable local-first stream shipping of SQLite-persisted data via NDJSON-over-HTTP."
    expectedValueType: "Boolean"
    allowableValues:
      - true
      - false
    isEditable: true
    isSecret: false

  - path: /services/streamShipping/endpointUrl
    defaultValue: ""
    description: "Stream shipping ingestion endpoint URL (e.g., https://example.com/ingest/v1/stream)."
    expectedValueType: "String"
    allowableValues: []
    isEditable: true
    isSecret: true

  - path: /services/streamShipping/maxBatchRows
    defaultValue: "500"
    description: "Maximum number of rows to ship per batch."
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/streamShipping/shipIntervalSeconds
    defaultValue: "10"
    description: "How often to attempt shipping (seconds)."
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/streamShippingHttp/timeoutSeconds
    defaultValue: "30"
    description: "HTTP timeout (seconds) for stream shipping uploads."
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/tempest/apiKey
    defaultValue: "00000000-0000-0000-0000-000000000000"
    description: "API Key for Tempest Weather Station"
    expectedValueType: "String"
    allowableValues: []
    isEditable: true
    isSecret: true

  - path: /services/tempest/stationId
    defaultValue: "0"
    description: "Station ID for Tempest Weather Station"
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/udpListener/preferredPort
    defaultValue: "50000"
    description: "UDP Port for listening for readings from Tempest Weather Station"
    expectedValueType: "Int32"
    allowableValues: []
    isEditable: true
    isSecret: false

  - path: /services/unitOfMeasure/airPressure
    defaultValue: "inch of mercury"
    description: "Unit that air pressure is provided in"
    expectedValueType: "String"
    allowableValues:
      - "atmosphere"
      - "bar"
      - "hectopascal"
      - "inch of mercury"
      - "kilopascal"
      - "millibar"
      - "pascal"
    isEditable: true
    isSecret: false

  - path: /services/unitOfMeasure/airTemperature
    defaultValue: "degree fahrenheit"
    description: "Unit that air temperature is provided in"
    expectedValueType: "String"
    allowableValues:
      - "degree fahrenheit"
      - "degree celsius"
    isEditable: true
    isSecret: false

  - path: /services/unitOfMeasure/batteryLevel
    defaultValue: "volt"
    description: "Unit that battery level is provided in"
    expectedValueType: "String"
    allowableValues:
      - "volt"
    isEditable: false
    isSecret: false

  - path: /services/unitOfMeasure/illuminance
    defaultValue: "lux"
    description: "Unit that illuminance is provided in"
    expectedValueType: "String"
    allowableValues:
      - "lux"
      - "candela"
    isEditable: false
    isSecret: false

  - path: /services/unitOfMeasure/lightningDistance
    defaultValue: "mile"
    description: "Unit that lightning distance is provided in"
    expectedValueType: "String"
    allowableValues:
      - "kilometer"
      - "meter"
      - "mile"
      - "nautical mile"
    isEditable: true
    isSecret: false

  - path: /services/unitOfMeasure/rainAccumulation
    defaultValue: "inch"
    description: "Unit that the amount of rain accumulation is provided in"
    expectedValueType: "String"
    allowableValues:
      - "centimeter"
      - "millimeter"
      - "inch"
    isSecret: false

  - path: /services/unitOfMeasure/solarRadiation
    defaultValue: "watt/square meter"
    description: "Unit that lightning distance is provided in"
    expectedValueType: "String"
    allowableValues:
      - "watt/square meter"
      - "kilowatt/square meter"
    isSecret: false

  - path: /services/unitOfMeasure/windSpeed
    defaultValue: "mile/hour"
    description: "Unit that lightning distance is provided in"
    expectedValueType: "String"
    allowableValues:
      - "kilometer/hour"
      - "knot"
      - "meter/second"
      - "mile/hour"
    isSecret: false